# This repo's real env examples live per-app:
#   - artifacts/pronto-curriculum/.env.example  (frontend, Vite)
#   - artifacts/api-server/.env.example         (backend, Express)
#
# Copy those into artifacts/pronto-curriculum/.env.local and
# artifacts/api-server/.env respectively and fill in your values.
# Never commit real credentials to git!
